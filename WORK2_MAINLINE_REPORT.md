# Work2 主线完成报告

## 结论

Work2 训练主线已实现并可运行：它使用真实数据、冻结的本地 Qwen2.5-1.5B 骨干模型，以及三个现成的冻结攻击模型。当前实现不再依赖已经移除的第一工作点生成器目录。

本里程碑证明的是实现正确性与运行连通性。正式低 FPR 评估、消融实验和完整对比实验均已明确延期。

## 核心决策

1. **攻击模型保持冻结。** DeepCorr300、m-DeepCorr、DeepCoFFEA 是 Work2 的反馈目标，不是需要重新构造或训练的对象。
2. **使用小型冻结骨干。** Qwen2.5-1.5B 无需量化或复杂模型并行，即可满足当前两卡运行方案。
3. **采用简单特征适配器。** 时序结构使用 Patch 线性投影，视觉结构使用两层二维 CNN。
4. **提示文本只作为语义锚点。** 数值模态经重编程映射到 Qwen embedding 空间；文本无需第二个模态编码器。
5. **生成加性物理扰动。** 输出仅能增加延迟或包大小的幅度；时间、大小单独模式由通道掩码强制执行。
6. **保留完整流量反馈。** DeepCorr 系列会在一条流的全部未来窗口写回后，再调用冻结攻击模型。

## 已完成的主线组件

- 真实 DeepCorr300 与 m-DeepCorr 的按流分组加载。
- 真实 DeepCoFFEA Tor/Exit 会话加载，以及 500/800 目标窗口。
- 时序 Patch 适配器。
- 学习型二维视觉适配器。
- 基于流量统计量的提示文本。
- 学习型原型与文本原型两种重编程模式。
- 冻结 Qwen embedding 与 hidden-state 路径。
- 正向时间与大小扰动约束。
- DeepCorr BCE 与 DeepCoFFEA 余弦 margin 目标。
- 时间、大小归一化 L2 开销。
- DeepCorr 系列的完整流量未来窗口写回。
- 优化器、梯度裁剪、学习率衰减、早停、最佳/最新 checkpoint 与断点恢复。
- 数据集、攻击模型 checkpoint 和基模快照的可迁移相对路径。

## 已完成验证

| 验证项目 | 结果 |
| --- | --- |
| JSON 配置校验 | 当前全部 Work2 配置通过 |
| Python 编译检查 | 通过 |
| 本地单元测试 | 通过；依赖 PyTorch 的测试在本地跳过 |
| 远程单元测试 | `/home/xilin/anaconda3/envs/deepcorr` 中 15/15 通过 |
| 按流分组写回梯度测试 | 远程 CPU 通过 |
| 冻结 Qwen 加载 | 本地 2.9GB 快照加载通过 |
| DeepCorr300 主线 smoke | 两张 GPU 上通过 |
| checkpoint 生成 | 通过 |

按流分组 smoke 的形状契约如下：

```text
历史窗口：       (8, 4, 96)
生成未来扰动：   (8, 48, 4)
来源 Tor 流量：  (2, 4, 300)
目标完整流量：   (2, 8, 300)
目标模型输出：   (2, 1)
```

这证明两条来源流量各自的四个窗口在同一批中生成，并被合并回两条完整目标流量。

## 远程执行策略

启动主线 smoke 前，已检查 GPU 状态和计算进程。物理 GPU 1 与 3 均为空闲、可用显存约 24GB；物理 GPU 0 与 2 存在其他用户进程，因此不使用。

实际映射为：

```text
CUDA_VISIBLE_DEVICES=3,1
逻辑 cuda:0 -> 物理 GPU 3 -> 冻结 Qwen
逻辑 cuda:1 -> 物理 GPU 1 -> Work2 可训练模块与冻结攻击模型
```

没有终止、修改或抢占其他用户的进程。

## 延期工作

以下内容不包含在本次完成声明中：

- 完整 DeepCorr300 训练。
- `10^-3` 与 `10^-4` FPR 实验。
- 仅时间、仅大小、无视觉、无提示文本等消融实验。
- 正式 m-DeepCorr 与 DeepCoFFEA 对比运行。
- 论文表格、图表与防御效果结论。

后续启动这些实验前，应再次检查 GPU 可用性，并只使用两张当前空闲显卡。
